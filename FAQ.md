# دانلود منیجر - سوالات متداول

## سؤالات متداول ❓

### Q1: چطور برنامه رو اجرا کنم؟
**جواب:** 
```bash
python3 main.py
```

### Q2: چطور وابستگی‌ها رو نصب کنم؟
**جواب:**
```bash
pip install -r requirements.txt
```

یا برای دبیان/اوبونتو:
```bash
sudo apt update
sudo apt install python3-pip
pip3 install -r requirements.txt
```

### Q3: فایل‌ها کجا ذخیره می‌شند؟
**جواب:** بصورت پیشفرض در پوشه `Downloads` ذخیره می‌شوند. می‌توانید از دکمه "انتخاب مسیر" برای تغییر مسیر استفاده کنید.

### Q4: میتونم چند فایل رو همزمان دانلود کنم؟
**جواب:** بله! تعداد دانلود‌های همزمان رو می‌تونید از SpinBox تنظیم کنید (پیشفرض: 3)

### Q5: اگر دانلود قطع شود چی می‌شود؟
**جواب:** می‌تونید دکمه "حذف" رو برای هر دانلود بزنید تا فایل حذف شود.

### Q6: آیا از Resume پشتیبانی می‌کنه؟
**جواب:** در نسخه فعلی خیر، اما این ویژگی در نسخه‌های آتی اضافه می‌شود.

### Q7: اگر خطای دانلود داشتم چی کار کنم؟
**جواب:** پیام خطا در ستون "وضعیت" نمایش داده می‌شود. اطلاعات لینک و اتصال اینترنت خود را بررسی کنید.

### Q8: منیجر پشتیبانی از FTP می‌کنه؟
**جواب:** فعلاً فقط HTTP/HTTPS پشتیبانی می‌شود.

## مشکلات رایج 🔧

### مشکل: خطای "No module named 'PyQt6'"
**حل:**
```bash
pip install PyQt6
```

### مشکل: خطای "Connection refused"
**حل:** 
- اتصال اینترنت خود را بررسی کنید
- لینک صحیح است یا خیر؟
- سرور فایل در دسترس است یا خیر؟

### مشکل: برنامه بسته می‌شود
**حل:**
```bash
python3 main.py
```
از ترمینال اجرا کنید تا پیام خطا را ببینید.

## درباره پروژه ℹ️

**نسخه:** 1.0.0  
**زبان:** Python 3.8+  
**لایسنس:** MIT  
**نویسندگان:** sadra-h68  

## مشارکت 🤝

اگر می‌خواهید بهبود دهید:
1. Repository رو Fork کنید
2. یک Branch جدید بسازید
3. تغییرات خود رو Commit کنید
4. یک Pull Request بسازید

## پشتیبانی 📞

برای گزارش مشکلات یا درخواست ویژگی جدید، یک Issue بسازید!

